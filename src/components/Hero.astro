<section class="responsive large-padding">
  <div class="grid middle-align">
    <div class="s12 m6 l6">
      <h1 class="display-medium text-gradient bold">Shojin App</h1>
      <h2 class="display-large bold" style="margin-top: 1rem; margin-bottom: 1.5rem;">
        AtCoderの精進を<br>スマホでも。
      </h2>
      <p class="body-large" style="color: var(--on-surface-variant); max-width: 500px;">
        問題閲覧、コーディング、テスト、提出まで。<br>
        スマホひとつで完結する競プロ環境。
      </p>
      <div class="space large"></div>
      <nav class="hero-buttons">
        <a href="https://github.com/Shojin-App/shojin_app/releases" 
           target="_blank" 
           class="button fill primary wave">
          <span>ダウンロード</span>
        </a>
        <a href="https://github.com/ImranR98/Obtainium" 
           target="_blank" 
           class="button border wave obtainium-btn">
          <span>Obtainiumで入手</span>
        </a>
      </nav>
    </div>
    <div class="s12 m6 l6 center-align relative">
      <div class="glow-bg"></div>
      <img src="/images/hero-mockup.png" 
           alt="Shojin App Mockup" 
           class="responsive mockup-image">
    </div>
  </div>
</section>

<style>
  .hero-buttons {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-buttons .button {
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .hero-buttons .button:hover {
    transform: translateY(-4px);
    box-shadow: var(--elevation-3);
  }

  .obtainium-btn {
    border-color: var(--outline);
    color: var(--on-surface);
  }

  .obtainium-btn:hover {
    border-color: var(--primary);
    background: var(--surface-container-high);
  }

  .mockup-image {
    max-height: 600px;
    border: 4px solid var(--surface-container);
    border-radius: var(--radius-extra-large);
    position: relative;
    z-index: 1;
    box-shadow: var(--elevation-3);
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1),
                box-shadow 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .mockup-image:hover {
    transform: scale(1.02);
    box-shadow: var(--elevation-4);
  }
</style>
